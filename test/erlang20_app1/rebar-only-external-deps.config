% this config is needed to specify dependencies
% that should be fetched from remote server
% it should not include local deps like batiscaph_probe
%
% this config needed to speed up docker image build
{deps, [{cowboy, "1.1.2"}]}.
